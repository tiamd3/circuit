use digicir::app::{ run, run_circuit };
use digicir::circuit::Circuit;
use digicir::signal::Signal;
use digicir::gate::GateType;
fn main() {
    // let mut circuit: Circuit<Signal> = Circuit::new_with_input(4);
    // circuit.add_gate(GateType::Xor(2), &[0, 1]);
    // circuit.add_gate(GateType::And(2), &[0, 1]);
    //println!("{:?}", run_circuit(circuit));
    //println!("{:?}", run());
}